import fs from 'node:fs';
import { chromium } from 'playwright';

const origin=process.env.R3_WP38D_ORIGIN ?? 'http://127.0.0.1:4173';
const outputDir=process.env.R3_WP38D_ARTIFACTS ?? 'artifacts/r3-wp3-8d';
const cities=[
  {id:'N-DUSSELDORF',name:'Dusseldorf',variant:'dusseldorf',position:[6.7735,51.2277],assetId:'wp3.8d-dusseldorf-selected',minimumFaces:1000,landmarks:['Rheinturm','Media Harbour / Rhine waterfront'],rotation:-10},
  {id:'N-STUTTGART',name:'Stuttgart',variant:'stuttgart',position:[9.1829,48.7758],assetId:'wp3.8d-stuttgart-selected',minimumFaces:1000,landmarks:['Fernsehturm Stuttgart','Neues Schloss / Schlossplatz roofline'],rotation:8},
  {id:'N-RENNES',name:'Rennes',variant:'rennes',position:[-1.6778,48.1173],assetId:'wp3.8d-rennes-selected',minimumFaces:1200,landmarks:['Parliament of Brittany','Rennes half-timbered old town'],rotation:-8}
];
fs.mkdirSync(outputDir,{recursive:true});const browser=await chromium.launch({headless:true});const page=await browser.newPage({viewport:{width:1600,height:1000},reducedMotion:'reduce'});
async function observe(city,lod){return page.evaluate(({id,expectedLod})=>{const d=window.__r3WorldMiniatures,nodes=window.__r3StrategicNodes??[];if(!d)throw new Error('world-miniature diagnostic unavailable');const o=d.objects.find(x=>x.id===id),n=nodes.find(x=>x.id===id);if(!o||!n)throw new Error(`missing city diagnostic ${id}`);return {...o,lod:d.lod,expectedLod,anchorErrorDegrees:Math.hypot(o.position[0]-n.position[0],o.position[1]-n.position[1])};},{id:city.id,expectedLod:lod});}
function validate(city,o,lod){if(!o.visible||o.lod!==lod)throw new Error(`${city.name} is not visible at ${lod} LOD: ${JSON.stringify(o)}`);if(o.cityVariant!==city.variant)throw new Error(`${city.name} variant mismatch: ${o.cityVariant}`);if(o.assetStatus!=='ready'||o.presentationModel!=='authored-gltf')throw new Error(`${city.name} did not use authored glTF at ${lod}: ${JSON.stringify(o)}`);if(o.assetId!==city.assetId||o.authoredFaceCount<city.minimumFaces)throw new Error(`${city.name} asset identity/detail contract failed: ${JSON.stringify(o)}`);if(o.anchorErrorDegrees!==0)throw new Error(`${city.name} geographic anchor changed`);if(!Number.isFinite(o.elevation)||o.clearance!==22)throw new Error(`${city.name} terrain grounding changed`);if(JSON.stringify(o.landmarks)!==JSON.stringify(city.landmarks))throw new Error(`${city.name} landmark metadata mismatch: ${JSON.stringify(o.landmarks)}`);}
try{
  await page.addInitScript(()=>{localStorage.setItem('future-conquest:intro-seen:v3','true');localStorage.setItem('future-conquest-tutorial-seen-v1','true');});await page.goto(`${origin}/?terrain=1`,{waitUntil:'domcontentloaded'});await page.getByRole('button',{name:'BEGIN CAMPAIGN',exact:true}).click();await page.locator('.startup-game-shell').waitFor({state:'visible'});await page.locator('[data-command-view="map"]').click();const host=page.locator('.r3-terrain-prototype');await host.waitFor({state:'visible',timeout:45000});await page.waitForFunction(()=>document.querySelector('.r3-terrain-prototype')?.getAttribute('data-status')==='ready'&&Boolean(window.__r3WorldMiniatures)&&Boolean(window.__r3TerrainMap),null,{timeout:45000});
  const layerControl=page.locator('details.r3-terrain-layer-control');await layerControl.evaluate(element=>{element.open=true;});for(const label of ['Friendly formations','Operations, threats and fronts','Ports']){const toggle=layerControl.getByLabel(label,{exact:true});if(await toggle.isChecked())await toggle.evaluate(element=>element.click());}await layerControl.evaluate(element=>{element.open=false;});await page.addStyleTag({content:'[data-r3-marker-id] { visibility: hidden !important; }'});
  const evidence={schemaVersion:2,cities:{}};
  for(const city of cities){await page.evaluate(({position,rotation})=>{const map=window.__r3TerrainMap;if(!map)throw new Error('terrain map diagnostic unavailable');map.jumpTo({center:position,zoom:5.35,pitch:51,bearing:rotation});},city);await page.waitForFunction(({id,assetId})=>{const d=window.__r3WorldMiniatures,o=d?.objects.find(x=>x.id===id);return d?.lod==='campaign'&&o?.assetStatus==='ready'&&o?.assetId===assetId&&o?.presentationModel==='authored-gltf';},{id:city.id,assetId:city.assetId},{timeout:20000});await page.waitForTimeout(350);const campaign=await observe(city,'campaign');validate(city,campaign,'campaign');await host.screenshot({path:`${outputDir}/${city.name.toLowerCase()}-authored-campaign.png`});await page.evaluate(({position,rotation})=>{const map=window.__r3TerrainMap;if(!map)throw new Error('terrain map diagnostic unavailable');map.jumpTo({center:position,zoom:8.1,pitch:50,bearing:rotation-8});},city);await page.waitForFunction(({id,assetId})=>{const d=window.__r3WorldMiniatures,o=d?.objects.find(x=>x.id===id);return d?.lod==='selected'&&o?.assetStatus==='ready'&&o?.assetId===assetId&&o?.presentationModel==='authored-gltf';},{id:city.id,assetId:city.assetId},{timeout:20000});await page.waitForTimeout(350);const selected=await observe(city,'selected');validate(city,selected,'selected');await host.screenshot({path:`${outputDir}/${city.name.toLowerCase()}-authored-selected.png`});evidence.cities[city.variant]={campaign,selected};}
  const fallbackCity={id:'N-LONDON',position:[-0.1276,51.5072],variant:'london'};await page.evaluate(({position})=>{const map=window.__r3TerrainMap;if(!map)throw new Error('terrain map diagnostic unavailable');map.jumpTo({center:position,zoom:4.45,pitch:44,bearing:0});},fallbackCity);await page.waitForFunction(({id})=>{const d=window.__r3WorldMiniatures,o=d?.objects.find(x=>x.id===id);return d?.lod==='theatre'&&o?.visible&&o?.presentationModel==='procedural-fallback';},{id:fallbackCity.id},{timeout:20000});await page.waitForTimeout(300);const theatre=await observe(fallbackCity,'theatre');if(theatre.cityVariant!=='london'||theatre.presentationModel!=='procedural-fallback'||theatre.anchorErrorDegrees!==0||theatre.clearance!==22||!Number.isFinite(theatre.elevation))throw new Error(`Pass 4 Theatre fallback changed: ${JSON.stringify(theatre)}`);evidence.theatreFallback=theatre;
  fs.writeFileSync(`${outputDir}/evidence.json`,`${JSON.stringify(evidence,null,2)}\n`);console.log(JSON.stringify(evidence,null,2));
}finally{await browser.close();}
